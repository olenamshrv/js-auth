<div class='field'>
  <label class='field__label' for='{{name}}'>
    {{label}}
  </label>
  <input
    oninput='
    console.log(this);
    {{action}}(this.name, this.value);'
    type={{type}}
    class='field__input'
    name='{{name}}'
    placeholder='{{placeholder}}'
  />
</div>